'use client';
import { AlertTriangle, CheckCircle2, Clock, ListTodo, TrendingUp } from 'lucide-react';
import { useSimulation } from '../context/SimulationContext';
export default function Metrics() { const { tasks, completedTasks, averageCompletionTime, conflictsResolved, totalDistance, fleetUtilization } = useSimulation(); const items = [{ label: 'Total Tasks', value: tasks.length, icon: ListTodo }, { label: 'Completed', value: completedTasks, icon: CheckCircle2 }, { label: 'Conflicts', value: conflictsResolved, icon: AlertTriangle }, { label: 'Avg Time', value: `${averageCompletionTime.toFixed(1)}s`, icon: Clock }, { label: 'Fleet Util', value: `${fleetUtilization}%`, icon: TrendingUp }, { label: 'Distance', value: `${totalDistance} cells`, icon: TrendingUp }]; return <div className="grid grid-cols-2 gap-px overflow-hidden rounded-2xl border border-zinc-800/90 bg-zinc-800/80 sm:grid-cols-3 lg:grid-cols-6">{items.map(({ label, value, icon: Icon }) => <div key={label} className="bg-zinc-900/90 p-4"><div className="flex items-center justify-between text-[10px] font-semibold uppercase tracking-wider text-zinc-500"><span>{label}</span><Icon size={14} /></div><div className="mt-2 font-mono text-xl font-bold tabular-nums text-zinc-50">{value}</div></div>)}</div>; }
