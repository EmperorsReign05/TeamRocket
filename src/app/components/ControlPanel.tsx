'use client';
import { AlertCircle, AlertTriangle, Play, Plus, RefreshCw, XOctagon } from 'lucide-react';
import { useSimulation } from '../context/SimulationContext';
export default function ControlPanel() { const { isSimulationRunning, handleCreateTask, handleToggleSimulation, handleSimulateConflict, handleSimulateDeadlock, handleFailRobot, handleBlockAisle, handleResetSimulation } = useSimulation(); const button = 'rounded-lg border px-3.5 py-1.5 text-[11px] font-bold uppercase tracking-wider transition-colors flex items-center gap-1.5 focus-visible:outline-2 focus-visible:outline-cyan-300'; return <section className="rounded-2xl border border-zinc-800/90 bg-zinc-900/80 p-4 shadow-[0_14px_36px_rgba(0,0,0,0.18)]"><h3 className="mb-3 flex items-center gap-2 text-[13px] font-semibold tracking-wide text-zinc-100"><span className="h-2 w-2 rounded-full bg-cyan-300" /> Control Panel</h3><div className="flex flex-wrap gap-2.5"><button onClick={handleCreateTask} className={`${button} border-cyan-300 bg-cyan-300 text-zinc-950 hover:bg-cyan-200`}><Plus size={14} /> Create Task</button><button onClick={handleToggleSimulation} className={`${button} border-emerald-400 bg-emerald-400 text-zinc-950 hover:bg-emerald-300`}><Play size={14} /> {isSimulationRunning ? 'Pause Simulation' : 'Start Simulation'}</button><button onClick={handleSimulateConflict} className={`${button} border-amber-400/50 bg-transparent text-amber-300 hover:bg-amber-400/10`}><AlertTriangle size={14} /> Simulate Conflict</button><button onClick={handleSimulateDeadlock} className={`${button} border-rose-400/50 bg-transparent text-rose-300 hover:bg-rose-400/10`}><XOctagon size={14} /> Simulate Deadlock</button><button onClick={handleFailRobot} className={`${button} border-zinc-700 bg-zinc-800 text-zinc-300 hover:bg-zinc-700`}><AlertCircle size={14} /> Fail AMR-02</button><button onClick={handleBlockAisle} className={`${button} border-violet-400/50 bg-transparent text-violet-300 hover:bg-violet-400/10`}><XOctagon size={14} /> Block Aisle</button><button onClick={handleResetSimulation} className={`${button} border-zinc-700 bg-transparent text-zinc-400 hover:bg-zinc-800`}><RefreshCw size={14} /> Reset</button></div></section>; }
