'use client';
import { useState } from 'react';
import { useSimulation } from '../context/SimulationContext';
export default function TaskTable() { const { tasks } = useSimulation(); const [filter, setFilter] = useState('all'); const visible = tasks.filter(task => filter === 'all' || task.status === filter); return <section className="overflow-hidden rounded-2xl border border-zinc-800/90 bg-zinc-900/80 shadow-[0_14px_36px_rgba(0,0,0,0.18)]"><div className="flex flex-wrap items-center justify-between gap-3 border-b border-zinc-800/80 bg-zinc-950/35 px-5 py-4"><div><div className="text-[10px] font-semibold uppercase tracking-[0.18em] text-cyan-300/70">Queue monitor</div><h2 className="text-lg font-semibold text-zinc-100">Task History</h2></div><select value={filter} onChange={event => setFilter(event.target.value)} className="rounded-lg border border-zinc-700 bg-zinc-900 px-3 py-2 text-xs text-zinc-300 outline-none focus:border-cyan-300"><option value="all">All statuses</option><option value="pending">Pending</option><option value="assigned">Assigned</option><option value="in-progress">In progress</option><option value="completed">Completed</option></select></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-xs"><thead className="bg-zinc-950/40 uppercase tracking-wider text-zinc-500"><tr><th className="px-5 py-3">Task</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Robot</th><th className="px-5 py-3">Pickup</th><th className="px-5 py-3">Drop-off</th><th className="px-5 py-3">Created</th></tr></thead><tbody className="divide-y divide-zinc-800/70">{visible.map(task => <tr key={task.id} className="text-zinc-300"><td className="px-5 py-4 font-semibold text-zinc-100">{task.id}</td><td className="px-5 py-4"><span className="rounded-full border border-cyan-400/30 bg-cyan-400/10 px-2.5 py-1 text-[10px] uppercase text-cyan-200">{task.status.replace('-', ' ')}</span></td><td className="px-5 py-4 font-mono">{task.assignedRobotId ?? 'Unassigned'}</td><td className="px-5 py-4 font-mono">({task.pickup.x}, {task.pickup.y})</td><td className="px-5 py-4 font-mono">({task.dropoff.x}, {task.dropoff.y})</td><td className="px-5 py-4 font-mono text-zinc-500">{new Date(task.createdAt).toLocaleTimeString('en-GB', { hour: '2-digit', minute: '2-digit' })}</td></tr>)}</tbody></table></div></section>; }
