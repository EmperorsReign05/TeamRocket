'use client';
import { useEffect, useRef } from 'react';
import { useSimulation } from '../context/SimulationContext';
export default function EventLog() { const { logs } = useSimulation(); const endRef = useRef<HTMLDivElement>(null); const logRef = useRef<HTMLDivElement>(null); const shouldFollow = useRef(true); useEffect(() => { if (shouldFollow.current) endRef.current?.scrollIntoView({ behavior: 'smooth' }); }, [logs]); return <section className="flex min-h-[230px] flex-1 flex-col overflow-hidden rounded-2xl border border-zinc-800/90 bg-zinc-900/80 shadow-[0_14px_36px_rgba(0,0,0,0.18)]"><div className="flex min-h-14 items-center justify-between border-b border-zinc-800/80 bg-zinc-950/35 px-5 py-3"><div><div className="text-[10px] font-semibold uppercase tracking-[0.18em] text-zinc-500">System stream</div><h3 className="text-sm font-semibold tracking-wide text-zinc-100">Event Log</h3></div><span className="flex items-center gap-2 text-[10px] font-bold uppercase tracking-wider text-emerald-300"><span className="h-1.5 w-1.5 animate-pulse rounded-full bg-emerald-300" />Live</span></div><div ref={logRef} onScroll={() => { const node = logRef.current; if (node) shouldFollow.current = node.scrollHeight - node.scrollTop - node.clientHeight < 24; }} className="flex-1 overflow-y-auto px-5 py-4 text-[12px] font-medium tracking-wide">{logs.map((log, index) => <div key={`${log.time}-${index}`} className="flex gap-4 pb-2 opacity-80"><span className="shrink-0 font-mono text-[11px] text-zinc-500">{log.time}</span><span className={log.type === 'warning' ? 'text-amber-400' : log.type === 'success' ? 'text-emerald-300' : 'text-zinc-300'}>{log.text}</span></div>)}<div ref={endRef} /></div></section>; }
