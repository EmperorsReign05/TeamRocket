'use client';
import { useSimulation } from '../context/SimulationContext';
import Metrics from '../components/Metrics';
export default function AnalyticsPage() { const { completedDurations, conflictsResolved, fleetUtilization } = useSimulation(); const bars = [32, 48, 42, 67, 54, 74, Math.max(18, Math.min(94, fleetUtilization))]; return <div className="mx-auto flex max-w-[1400px] flex-col gap-5"><div><div className="text-[10px] font-semibold uppercase tracking-[0.2em] text-cyan-300/70">Operations / Analytics</div><h2 className="mt-1 text-2xl font-semibold text-zinc-100">Fleet Analytics</h2><p className="mt-1 text-sm text-zinc-500">A lightweight operational view of throughput, conflicts, and utilization.</p></div><Metrics /><div className="grid gap-5 lg:grid-cols-2"><section className="rounded-2xl border border-zinc-800/90 bg-zinc-900/80 p-5"><h3 className="font-semibold text-zinc-100">Completion time trend</h3><div className="mt-8 flex h-48 items-end gap-3 border-b border-l border-zinc-700 px-4">{bars.map((height, index) => <div key={index} className="group relative flex-1 rounded-t bg-cyan-400/70 transition-all hover:bg-cyan-300" style={{ height: `${height}%` }}><span className="absolute -top-6 left-1/2 -translate-x-1/2 text-[10px] text-zinc-500">{index + 1}</span></div>)}</div><div className="mt-3 flex justify-between text-[10px] uppercase tracking-wider text-zinc-600"><span>Older</span><span>Latest</span></div></section><section className="rounded-2xl border border-zinc-800/90 bg-zinc-900/80 p-5"><h3 className="font-semibold text-zinc-100">Conflict resolution</h3><div className="mt-8 flex items-center justify-center"><div className="relative flex h-44 w-44 items-center justify-center rounded-full border-[18px] border-amber-400/70"><div className="absolute inset-[-18px] rounded-full border-[18px] border-transparent border-t-rose-400/70 border-r-rose-400/70" /><div className="text-center"><div className="font-mono text-3xl font-bold text-zinc-100">{conflictsResolved}</div><div className="text-[10px] uppercase tracking-wider text-zinc-500">resolved</div></div></div></div></section></div><section className="rounded-2xl border border-zinc-800/90 bg-zinc-900/80 p-5"><h3 className="font-semibold text-zinc-100">Observed completion times</h3><div className="mt-4 flex flex-wrap gap-3">{completedDurations.length ? completedDurations.map((duration, index) => <span key={index} className="rounded-lg border border-emerald-400/30 bg-emerald-400/10 px-3 py-2 font-mono text-xs text-emerald-300">Task {index + 1}: {duration.toFixed(1)}s</span>) : <span className="text-sm text-zinc-500">No completed tasks in this session yet.</span>}</div></section></div>; }
