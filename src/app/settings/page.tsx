'use client';
import { useSimulation } from '../context/SimulationContext';
export default function SettingsPage() { const { tickMs, setTickMs, handleResetSimulation, isSimulationRunning } = useSimulation(); return <div className="mx-auto flex max-w-3xl flex-col gap-5"><div><div className="text-[10px] font-semibold uppercase tracking-[0.2em] text-cyan-300/70">System / Configuration</div><h2 className="mt-1 text-2xl font-semibold text-zinc-100">Settings</h2><p className="mt-1 text-sm text-zinc-500">Tune the local simulation without changing the warehouse model.</p></div><section className="rounded-2xl border border-zinc-800/90 bg-zinc-900/80 p-5"><label className="flex items-center justify-between gap-5"><span><b className="block text-sm text-zinc-100">Simulation tick speed</b><small className="text-xs text-zinc-500">Current interval: {tickMs}ms {isSimulationRunning ? '(running)' : '(paused)'}</small></span><select value={tickMs} onChange={event => setTickMs(Number(event.target.value))} className="rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm text-zinc-200 outline-none focus:border-cyan-300"><option value="500">Fast · 500ms</option><option value="700">Balanced · 700ms</option><option value="1000">Slow · 1000ms</option></select></label></section><section className="rounded-2xl border border-rose-400/20 bg-rose-400/[0.04] p-5"><h3 className="font-semibold text-zinc-100">Session controls</h3><p className="mt-1 text-sm text-zinc-500">Reset robots, tasks, logs, blocked cells, and metrics to the initial demo state.</p><button onClick={handleResetSimulation} className="mt-4 rounded-lg border border-rose-400/50 px-4 py-2 text-xs font-bold uppercase tracking-wider text-rose-300 hover:bg-rose-400/10">Reset simulation</button></section></div>; }
